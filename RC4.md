RC4 is a **stream cipher** designed in 1987 by Ron Rivest.  
It was widely used in protocols like:

- WEP
    
- WPA (early versions)
    
- TLS (now deprecated with RC4)
    

RC4 generates a **keystream** that is XORed with plaintext to produce ciphertext.