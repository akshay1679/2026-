light weight application 
👉 **Container = a portable box that runs your app**
we will be able to make sure that the resource could be able to run 


## 🧠 Cloud vs Container (Super Easy Analogy)

Imagine food delivery:

- **Cloud** → the kitchen building
- **Container** → the lunch box with food inside

The kitchen provides space & power.  
The lunch box contains the actual meal.

cloud containing container  → will be able to contain all the resour ce so that which resource we are using it will work on that resource


A **container** is a lightweight, isolated environment that runs a single application and all the necessary components to support it. Instead of bringing a whole separate operating system, a container borrows the core of the existing system by running on the kernel, which is the part of an operating system that communicates with the hardware and manages resources such as memory and running programs.