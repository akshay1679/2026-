GRUB (Grand Unified Bootloader) is ==the default, highly customizable bootloader for most Linux distributions, acting as the initial program that loads the operating system kernel into memory after the BIOS/UEFI initializes==.

supports unlimited boot entries
