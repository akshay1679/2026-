The shell lives in **user space** (not inside the kernel). It is a program whose job is to let humans control the system.

Popular shells include:

- Bash
- Zsh
- Fish

What the shell does:

- Reads your commands.
- Starts programs.
- Connects programs together (pipes like `|`).
- Handles environment variables, scripts, automation.

